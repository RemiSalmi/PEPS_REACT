const initState = {
    answers: [
        {idAnswer:8,answer:"The answer",idCategory:2,idUser:29},
    ]
}

const answerReducer = (state = initState, action) =>{
    return state
}

export default answerReducer